@model Rezervist.Models.Rezervasyon

@{
    ViewData["Title"] = "Ödeme ve Folyo İşlemleri";
}

<div class="container mt-4">
    <div class="row">
        
        <div class="col-md-7">
            <div class="card shadow border-primary h-100">
                <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
                    <h5 class="mb-0"><i class="fa-solid fa-cash-register me-2"></i>Ödeme Ekranı</h5>
                    <span class="badge bg-white text-primary">
                        Oda: @(Model.Oda != null ? Model.Oda.OdaNumarasi : "?")
                    </span>
                </div>
                <div class="card-body">
                    
                    <div class="alert alert-light border">
                        <h4 class="text-primary mb-0">
                            <i class="fa-solid fa-user me-2"></i>
                            @(Model.Misafir != null ? Model.Misafir.AdSoyad : "Misafir Bilgisi Bulunamadı")
                        </h4>
                        <small class="text-muted">
                            Giriş: @Model.GirisTarihi.ToString("dd.MM.yyyy") | 
                            Çıkış: @Model.CikisTarihi.ToString("dd.MM.yyyy")
                        </small>
                    </div>

                    <form asp-action="OdemeAl" method="post">
                        <input type="hidden" name="id" value="@Model.RezervasyonID" />

                        <div class="mb-3">
                            <label class="fw-bold">Toplam Ödenecek Tutar</label>
                            <div class="input-group input-group-lg">
                                <span class="input-group-text bg-success text-white">₺</span>
                                <input type="number" name="toplamTutar" class="form-control fw-bold text-success" 
                                       value="@Model.ToplamTutar.ToString("0.##").Replace(",",".")" step="0.01" />
                            </div>
                            <small class="text-muted">* Oda ücreti ve ekstralar dahildir.</small>
                        </div>

                        <div class="mb-4">
                            <label class="fw-bold">Ödeme Yöntemi</label>
                            <select name="odemeTuru" class="form-select form-select-lg">
                                <option value="Kredi Kartı">💳 Kredi Kartı</option>
                                <option value="Nakit">💵 Nakit</option>
                                <option value="Havale/EFT">🏦 Havale / EFT</option>
                            </select>
                        </div>

                        <button type="submit" class="btn btn-success w-100 btn-lg mb-3">
                            <i class="fa-solid fa-check-circle me-2"></i>ÖDEMEYİ ONAYLA
                        </button>
                    </form>

                    <hr />

                    <div class="d-grid">
                        <a asp-action="FaturaYazdir" asp-route-id="@Model.RezervasyonID" target="_blank" class="btn btn-dark">
                            <i class="fa-solid fa-print me-2"></i>Faturayı Yazdır
                        </a>
                    </div>
                    
                    <div class="text-center mt-3">
                        <a asp-action="Konaklayanlar" class="text-muted text-decoration-none">
                            <i class="fa-solid fa-arrow-left me-1"></i>Listeye Geri Dön
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-md-5">
            <div class="card shadow border-warning h-100">
                <div class="card-header bg-warning text-dark">
                    <h5 class="mb-0"><i class="fa-solid fa-martini-glass me-2"></i>Ekstra Harcamalar</h5>
                </div>
                <div class="card-body">
                    
                    <form asp-action="HarcamaEkle" method="post" class="mb-4">
                        <input type="hidden" name="id" value="@Model.RezervasyonID" />
                        
                        <label class="form-label small fw-bold">Hızlı Ekle:</label>
                        <div class="input-group mb-2">
                            <select name="urunAdi" class="form-select">
                                <option value="Oda Servisi (Yemek)">🍔 Yemek</option>
                                <option value="İçecek/Minibar">🥤 İçecek</option>
                                <option value="SPA / Masaj">💆 SPA</option>
                                <option value="Kuru Temizleme">👔 Kuru Temizleme</option>
                                <option value="Transfer">🚕 Transfer</option>
                            </select>
                            <input type="number" name="fiyat" class="form-control" placeholder="Tutar" required style="max-width: 100px;">
                            <button class="btn btn-outline-dark" type="submit">Ekle</button>
                        </div>
                    </form>

                    <h6 class="border-bottom pb-2">Harcama Detayları</h6>
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item d-flex justify-content-between align-items-center bg-light">
                            <span><i class="fa-solid fa-bed me-2"></i>Konaklama Ücreti</span>
                            <span class="fw-bold text-muted">Dahil</span>
                        </li>

                        @if (Model.Harcamalar != null && Model.Harcamalar.Any())
                        {
                            @foreach (var harcama in Model.Harcamalar)
                            {
                                <li class="list-group-item d-flex justify-content-between align-items-center">
                                    <span>@harcama.UrunAdi</span>
                                    <span class="badge bg-secondary rounded-pill">@harcama.Tutar ₺</span>
                                </li>
                            }
                            
                            <li class="list-group-item text-end pt-3">
                                <small class="text-muted">Ekstralar Toplamı:</small>
                                <div class="fw-bold text-danger fs-5">
                                    @Model.Harcamalar.Sum(x => x.Tutar).ToString("C2")
                                </div>
                            </li>
                        }
                        else
                        {
                            <div class="alert alert-info mt-3 small text-center">
                                Henüz ekstra bir harcama eklenmemiş.
                            </div>
                        }
                    </ul>

                </div>
            </div>
        </div>
    </div>
</div>